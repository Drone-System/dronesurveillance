# CORE

## To use core there needs to be a .env config that should have the following fields

```
GRPC_REMOTE_IP="" #should be the ip of the grpc server aka proxy
GRPC_REMOTE_PORT=
NAME=""
DB_HOST=""
DB_PORT=""
DB_USER=""
DB_PASSWORD=""
DB_NAME=""
```